apiVersion: v1
kind: Service
metadata:
  name: {{ include "search.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "search.name" . }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 8001
